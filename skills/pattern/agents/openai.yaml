interface:
  display_name: "Pattern"
  short_description: "Build tracked, runnable pattern codebases under patterns/."
  default_prompt: "Use $pattern to turn a recurring engineering lesson into a tracked, standalone codebase at patterns/<pattern-name>/."
