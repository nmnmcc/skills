interface:
  display_name: "Pattern"
  short_description: "Build runnable repositories that teach project patterns."
  default_prompt: "Use $pattern to turn a recurring engineering lesson into an independent, runnable teaching repository."
