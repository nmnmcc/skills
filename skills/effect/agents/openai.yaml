interface:
  display_name: "Effect"
  short_description: "Make effects and execution boundaries explicit."
  default_prompt: "Use $effect to make effects, dependencies, failures, resources, and execution boundaries explicit while following the project's existing tools."
