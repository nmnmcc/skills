interface:
  display_name: "Effect"
  short_description: "Make effects and execution boundaries explicit."
  default_prompt: "Use $effect to keep supported work inside the project's effect system, and ask for my explicit approval before any unavoidable non-effectful escape hatch."
