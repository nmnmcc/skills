interface:
  display_name: "Effect"
  short_description: "Make every effect model and boundary explicit."
  default_prompt: "Use $effect to keep the project's effect model and execution boundaries explicit, and ask for my approval before any unavoidable escape hatch."
