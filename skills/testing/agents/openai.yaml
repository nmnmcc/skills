interface:
  display_name: "Testing"
  short_description: "Seek evidence that distinguishes credible software faults."
  default_prompt: "Use $testing to decide what evidence is needed and add tests only when they can distinguish required behavior from a credible fault."
