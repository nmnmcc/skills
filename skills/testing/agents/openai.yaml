interface:
  display_name: "Testing"
  short_description: "Test observable behavior through real runtime boundaries."
  default_prompt: "Use $testing to write maintainable tests for observable runtime behavior."
